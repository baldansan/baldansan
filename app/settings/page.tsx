import { SettingsAppView } from "@/components/mobile/settings-app-view";

export const metadata = {
  title: "Тохиргоо — Бөөндөө Сурцгаая",
  description: "Account, курс сонголт, app тохиргоо.",
};

export default function SettingsPage() {
  return <SettingsAppView />;
}
