import { TeacherSetupForm } from "@/components/teacher/teacher-setup-form";

export const metadata = {
  title: "Багшийн profile — Teacher setup",
  description: "Create teacher profile for classroom management.",
};

export default function TeacherSetupPage() {
  return <TeacherSetupForm />;
}
